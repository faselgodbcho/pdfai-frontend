export default function Session() {
  return (
    <main
      className="flex items-center justify-center px-4 bg-background"
      style={{ minHeight: "calc(100vh - 60px)" }}
    >
      chat session something
    </main>
  );
}
